{
  "name": "reacthead",
  "version": "0.1.1",
  "description": "Modify the page <head> with React jsx tags in any component. Set title, Facebook OpenGraph, metadata, etc.",
  "main": "./bin/reacthead.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [
		"react",
		"head",
		"react-head",
		"title",
		"opengraph",
		"og",
		"link",
		"meta"
	],
	"author": "Daniel Winterstein",
	"license": "MIT",
	"babel": {
    "presets": [
      "es2015"
    ]
  },
  "devDependencies": {
    "babel-cli": "^6.2.4",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.2.0"
  },
  "scripts": {
    "build": "babel src --out-dir bin",
    "watch": "babel src --out-dir bin --watch",
    "prepublish": "npm run build"
  }
}
